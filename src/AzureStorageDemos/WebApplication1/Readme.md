# Azure Table Storage

1. Install `Azure.Data.Tables` Nuget Package

2. Configure the Connection String in `appsettings.json` file

3. Define the Entity Model

4. Defined the TableClientService

5. Register the TableClientService into the DI Container in `Program.cs` 

6. Create the MVC Controller 

7. Create the View for the Action Method 

8. Configure the Menu to navigate to the Controller Action Method